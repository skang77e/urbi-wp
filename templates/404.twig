{% extends 'base.twig' %}

{% block content %}
  <!-- Provided by templates/404.twig -->
  {% embed '@layouts/content/content.twig' with {
    'has_constrain': true
  } %}
    {% block content %}
      {% include '@components/page-title/page-title.twig' with {
        'page_title': __('Uh-Oh...Page not found! ', 'urbi')
      } %}
      <h2><a href="{{ site.url }}">{{ __('Return to Homepage','urbi') }}</a></h2>
    {% endblock %}
  {% endembed %}
{% endblock %}
